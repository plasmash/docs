# Topology & nodes

!!! info "Work in progress"
    This page is being written. The outline below is what it will cover — the knowledge exists, it just needs publishing here.

How the logical platform maps onto physical machines.

## Planned contents

- Zones as the units of the topology
- `distributes` (zone → component) and `allocates` (node → zone)
- Matching compute profiles to zones (GPU, storage, balanced)
- Nodes and the platform skeleton

!!! note "See also"
    Operational side: [Nodes & topology](../operate/nodes.md). Commands: [`plasmactl zone`](../cli/zone.md).
