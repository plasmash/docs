# Quickstart

!!! info "Work in progress"
    This page is being written. The outline below is what it will cover — the knowledge exists, it just needs publishing here.

Go from nothing to a running platform in a handful of commands.

## Planned contents

- Install `plasmactl`
- `platform:create` — scaffold a platform
- Declare packages in `compose.yaml`
- `platform:up` — bring it up on your infrastructure
- Verify with `platform:graph`

!!! note "See also"
    Until then, see [Install](install.md) and [Deploying](operate/deploying.md).
