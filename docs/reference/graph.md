# Dependency graph

!!! info "Work in progress"
    This page is being written. The outline below is what it will cover — the knowledge exists, it just needs publishing here.

The model behind `platform:graph` — every platform as a typed, directed graph.

## Planned contents

- Node types: platform, model, package, zone, node, component, variable
- Edges: executes, materializes, composes, distributes, allocates, orchestrates
- Querying and visualizing the graph
- Impact analysis
