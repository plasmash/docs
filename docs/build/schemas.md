# Entity schemas

!!! info "Work in progress"
    This page is being written. The outline below is what it will cover — the knowledge exists, it just needs publishing here.

How the ontology stays a stable contract while it evolves.

## Planned contents

- Protobuf as the source of truth (`files/*.proto`)
- Primary keys and proto package conventions
- JSON Schema generation for build-time validation
- The runtime schema registry
- Schema changes as breaking changes
