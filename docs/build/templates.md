# Templates & filters

!!! info "Work in progress"
    This page is being written. The outline below is what it will cover — the knowledge exists, it just needs publishing here.

Jinja2 templates and the machine filters Plasma adds.

## Planned contents

- Keeping templates env-agnostic and data-driven
- `cluster_dns_name`, `build`/`state`, `image_uri`/`image_path`
- `requires_lookup` and component-type filters
- Executable template files and Docker COPY permissions
