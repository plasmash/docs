# Automation & structured output

!!! info "Work in progress"
    This page is being written. The outline below is what it will cover — the knowledge exists, it just needs publishing here.

Drive `plasmactl` from scripts and agents.

## Planned contents

- `--json` / `--yaml` structured output
- Action result schemas in `action.yaml`
- Native plugin vs package action
- Building automation on the dependency graph
