# Nodes & topology

!!! info "Work in progress"
    This page is being written. The outline below is what it will cover — the knowledge exists, it just needs publishing here.

Managing the machines and how zones distribute onto them.

## Planned contents

- Node files as the single source of truth
- `platform.yaml` constants vs derived state
- Allocating nodes to zones
- Host and group inventory variables

!!! note "See also"
    Commands: [`plasmactl node`](../cli/node.md), [`plasmactl zone`](../cli/zone.md).
