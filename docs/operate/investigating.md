# Investigating a platform

!!! info "Work in progress"
    This page is being written. The outline below is what it will cover — the knowledge exists, it just needs publishing here.

The one correct way to understand a running platform: the graph.

## Planned contents

- `platform:graph` — overview, JSON, per-node queries, reverse deps, Mermaid
- `platform:check` — validate consistency
- `platform:impact` — blast radius of a change
- `component:list` / `component:query`
- Why filesystem exploration is the wrong tool
