# Platform lifecycle

!!! info "Work in progress"
    This page is being written. The outline below is what it will cover — the knowledge exists, it just needs publishing here.

From a `compose.yaml` to a running platform.

## Planned contents

- `model:compose` — fetch and merge packages
- `model:prepare` — build the Ansible runtime
- `model:bundle` — produce the deployable `.pm` artifact
- `platform:up` — the full bump → compose → prepare → deploy workflow
